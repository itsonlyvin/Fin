import 'package:flutter/material.dart';

class AskLeave extends StatelessWidget {
  const AskLeave({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("AskLeave"),
    );
  }
}

import 'package:flutter/material.dart';

class NotificationsAdmin extends StatelessWidget {
  const NotificationsAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("NotificationsAdmin"),
    );
  }
}
